targetNamespace: ''
cleanNamespace: false
bucket: {}
parameters: {}
environment:
  ENV: "${VERBOSITY}"
  VERBOSITY: "${VERBOSITY}"
  COURIER_AUTH_TOKEN: "${COURIER_AUTH_TOKEN}"
packages:
  - name: contact
    shared: false
    clean: false
    environment: {}
    parameters: {}
    actions:
      - name: contact
        clean: false
        binary: false
        web: true
        webSecure: false
        annotations: {}
        limits: {}
        main: ''
        runtime: 'go:default'
        parameters: {}
        environment: {}
