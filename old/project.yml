parameters: {}
environment:
  ENV: "${ENV}"
  VERBOSITY: "${VERBOSITY}"
  COURIER_AUTH_TOKEN: "${COURIER_AUTH_TOKEN}"
packages:
  - name: contact
    environment: {}
    parameters: {}
    actions:
      - name: newContact
        main: ''
        runtime: 'go:default'
        parameters: {}
        environment: {}
